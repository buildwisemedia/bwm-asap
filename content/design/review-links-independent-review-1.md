## Verdict: Source-review ACCEPT, with one real drift finding to fix before the next generator run

The narrow patch does what it claims. I verified the content directly from source, not from the claimed pass. This is source-review acceptance only — not client approval, not production approval.

**What I confirmed myself:**

- **Link assignments are correct.** All 15 animal-page links check out: each of the 5 pages carries Mark Carroll → `31rhtxDM23w1LT7c7`, Kelsey Monaghan → `fdpS9PhMoSqUwNtH8`, Fred Perry → `bxMQCXrwU2gLZQ857`, matching name-for-name against `content/google-review-permalinks.json`. The homepage has exactly 8 `review-direct` anchors (5 desktop at `index.html:258,270,282,306,318` + 3 mobile duplicates at `:332,345,358`), each URL matching its named author. All 5 URLs in the ledger are distinct.
- **No nested anchors.** Every converted wrapper contains only images, divs, and text. The `video-review` wrappers (`index.html:373–381`) keep their inner lightbox anchors and were correctly left as divs.
- **No layout regression risk from div→anchor.** `.review-wrapper` carries `display:flex` on the class itself (webflow CSS), so the swap preserves layout. The new inline style adds `color:inherit`, `text-decoration:none`, and a visible `:focus-visible` outline.
- **No invented Charlie URL.** His desktop card (`index.html:294–305`) stays a plain div, and the ledger's `unresolved` block documents why. QA line 150 enforces this.
- **General listing links stay general.** The rodent page keeps exactly one occurrence of the `cid=` listing URL (the button), so the new QA check at `qa/asap-close-qa.mjs:146` holds. Other animal pages keep the long listing URL on their buttons.
- **New QA checks are sound.** Lines 138–150 are real assertions anchored to the JSON ledger, and I re-verified each one statically against the files. The old blanket check ("every link reuses the homepage destination") was correctly replaced. The `git show` base-page pin (line 309) reads a historical commit, unaffected by this patch. Manifest hash pins for changed files match `HASHES.json` across all three manifests.

**Finding 1 (medium, latent) — generator drift onto city pages.** `reviews()` at `tools/build-asap-close.mjs:430–440` is shared: `renderCity` calls it at line 534, and legacy mode writes the five `wildlife-removal-*` city pages plus pest at lines 591–593. The new `individualReviewUrls[name] || destination` lookup means the next legacy-mode regeneration will put exact review links on city-page review cards — pages that were not regenerated, not in this diff, and not in this snapshot. Two consequences: committed city HTML in the full repo no longer reproduces byte-for-byte from the generator (the reproducibility property the predecessor verification asserted), and the next legacy rebuild silently propagates this change outside any reviewed scope. Fix either way: regenerate the city pages under review, or scope the exact-link lookup to animal pages. Verify committed city-page state in the full repo — this snapshot omits those files, so I could not confirm it here.

**Finding 2 (low) — ledger excerpt flag inconsistency.** In `content/google-review-permalinks.json`, Yashica Marshall's entry carries `"excerpt": true` (line 37), but Benjamin Olmstead's homepage card text ("Went above and beyond… after rats.") is also a shortened excerpt of his recorded review text (lines 27–31) and carries no flag. No link impact; just makes the evidence file slightly inconsistent about its own convention.

**Limits of this review:** shell execution was denied in this session, so I could not independently recompute the sha256 values — hash claims are verified as internally consistent across the manifests and `HASHES.json`, which share provenance with the patch author. I also did not re-run the deterministic QA or render the page; the structural claims Root made (8 links, focus ring, full-card size) are consistent with the static HTML and CSS I read.

**Open holds, preserved and unchanged by this acceptance:** Charlie Cichetti's exact review URL remains unresolved (deliberately, no URL invented). Composed-page/client acceptance is not renewed and remains pending, as do the pre-existing holds: human signoff on design, the mobile/tablet navigation direction, and the full N4 production cutover set (sitemap, indexability, redirects, canonicals, production form delivery, deployment, live QA). `production_clear`, `preview_authorized`, and `send_authorized` all remain false. I am not requesting deployment approval.
