# Rodent primary-action evidence

The page exposes the same truthful two-action choice in the hero and contact path:

1. Call `770-691-3636` through `tel:+17706913636`.
2. Request an inspection through the attributed local-review fixture.

The call action remains visible in the mobile header. The form identifies its page, page type, service, attribution fields, consent state, and `fixture-only` integration state. Neither action is represented as a diagnosis, guaranteed availability, or live delivery proof.
