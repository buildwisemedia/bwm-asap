# Client report tier drift handoff

Status: `ready-for-reroute` — no production renderer change was made in this
repository.

## Action

Route the implementation to the canonical Client Performance Report owner:
`skills/client-performance-report/run.py` in the Buildwise Brain. Remove the
legacy alias entry `"ascend": "ascend-pro"` from `TIER_LEGACY_MAP` so the
current `ascend` tier remains `ascend`. Preserve the still-valid `pilot` and
`ascend_lite` aliases.

Add renderer regression coverage for this normalization matrix:

| Input tier | Expected rendered tier |
| --- | --- |
| `ascend` | `ascend` |
| `ascend-pro` | `ascend-pro` |
| `pilot` | `ascend-pilot` |
| `ascend_lite` | `ascend` |

Publish the canonical skill change through its normal install/sync path so the
installed `~/.claude/skills/client-performance-report/run.py` copy cannot drift.

## Risk

The selected `asap-pest-wildlife` repository contains the client website, but
no `client_reports` renderer, tier resolver, or alias map. Adding tier logic
here would create a disconnected second source of truth and would not change
rendered reports. Before removing the legacy alias in the owning repository,
confirm there are no unmigrated clients whose old `ascend` value still means
Ascend Pro.

## Verification evidence

- The target repository has no executable occurrence of `client_reports`,
  `TIER_LEGACY_MAP`, or `ascend-pro`.
- The canonical renderer and its installed copy both currently contain
  `"ascend": "ascend-pro"` in `TIER_LEGACY_MAP`.
- BWM canonical product data defines Ascend and Ascend Pro as separate current
  products, so a current `ascend` value must not be promoted by a legacy alias.
- Required owning-repository test: normalize each matrix input above and assert
  the selected cadence/headline uses the expected tier.

## Rollback/abort path

Revert the single alias-map change and its regression test if the pre-change
client-tier audit finds an unmigrated legacy Pro client. Do not compensate by
changing client rows from this website repository.

## Work item id

`5266adf9-1155-43c5-b55d-01e29a466840`
